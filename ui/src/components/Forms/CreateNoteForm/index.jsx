import React from "react";
import { Row, Col, Form } from "react-bootstrap";

function CreateNoteForm({ values, errors, touched, handleBlur, handleChange }) {
  return <div>CreateNoteForm</div>;
}

export default CreateNoteForm;
